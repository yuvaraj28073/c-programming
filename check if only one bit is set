#include <stdio.h>

int main() {
    int n;

    printf("Enter a number: ");
    scanf("%d", &n);

    if (n > 0 && (n & (n - 1)) == 0) {
        printf("Only one bit is set (Yes)\n");
    } 
    else {
        printf("More than one bit is set (No)\n");
    }

    return 0;
}
